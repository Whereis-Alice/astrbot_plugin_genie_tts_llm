{
  "enable_group_tts_by_default": {
    "title": "默认开启全部群语音",
    "description": "所有群默认开启",
    "hint": "开启后无需配置群组白名单或执行 /ttg。群组黑名单仍优先；/ttg-q 可临时关闭当前群，插件重启后恢复默认。",
    "type": "bool",
    "default": false
  },
  "group_whitelist": {
    "title": "群组白名单",
    "description": "启动时自动开启",
    "hint": "当“默认开启全部群语音”关闭时生效。填写群号，例如：123456789。",
    "type": "list",
    "items": {
      "type": "string"
    },
    "default": []
  },
  "group_blacklist": {
    "title": "群组黑名单",
    "description": "彻底禁用群",
    "hint": "黑名单中的群组将禁用语音合成的所有功能和指令。填写群号，例如：123456789。",
    "type": "list",
    "items": {
      "type": "string"
    },
    "default": []
  },
  "tts_servers": {
    "title": "TTS 服务器列表",
    "description": "Genie TTS 根地址",
    "hint": "填写 Space 或本地服务根地址，不要带 /tts。支持多个地址，插件会按顺序尝试。",
    "type": "list",
    "items": {
      "type": "string"
    },
    "default": [
      "https://your-hf-space.hf.space"
    ]
  },
  "default_character": {
    "title": "默认角色名称",
    "description": "默认角色",
    "hint": "必须与 Genie TTS 服务端已加载的角色名一致，例如 kisaki、aoi、oka。",
    "type": "string",
    "default": "kisaki"
  },
  "default_emotion_name": {
    "title": "默认感情名称",
    "description": "默认感情",
    "hint": "固定情感模式下使用。必须已通过 /注册感情 注册，或已导入 emotions.json。",
    "type": "string",
    "default": "开心"
  },
  "tts_default_language": {
    "title": "TTS 默认语言",
    "description": "默认语言代码",
    "hint": "当感情没有单独指定语言时使用。jp=日语，zh=中文，en=英语。",
    "type": "string",
    "default": "jp",
    "options": [
      "jp",
      "zh",
      "en"
    ]
  },
  "enable_translation": {
    "title": "启用目标语言转换",
    "description": "外语TTS总开关",
    "hint": "开启后会把回复转换为 TTS 需要的目标语言；具体由“外语TTS准备方式”决定是主 LLM 注入翻译，还是 Provider 翻译。若模型直接支持原文语言，可关闭。",
    "type": "bool",
    "default": true
  },
  "enable_translation_debug_log": {
    "title": "记录翻译结果",
    "description": "输出到日志",
    "hint": "开启后会在 AstrBot 日志中输出原文和最终送入 TTS 的合成文本，方便排查翻译或标签提取问题。",
    "type": "bool",
    "default": false
  },
  "enable_tts_text_cleaning": {
    "title": "启用 TTS 文本清洗",
    "description": "正则清洗文本",
    "hint": "在请求 TTS 前按下方正则清理文本，常用于去掉括号内心理描写或舞台说明。",
    "type": "bool",
    "default": false
  },
  "tts_text_clean_regex": {
    "title": "TTS 清洗正则",
    "description": "清洗表达式",
    "hint": "仅在启用文本清洗时生效。默认删除中英文括号、方括号、花括号、尖括号及其中内容。",
    "type": "string",
    "default": "\\([^()]*\\)|（[^（）]*）|\\[[^\\[\\]]*\\]|【[^【】]*】|\\{[^{}]*\\}|｛[^｛｝]*｝|<[^<>]*>|《[^《》]*》"
  },
  "tts_timeout_seconds": {
    "title": "TTS 超时时间",
    "description": "单次请求秒数",
    "hint": "单次 TTS 请求的超时时间。Hugging Face 免费 Space 较慢，建议 60 到 300 秒。",
    "type": "int",
    "default": 120
  },
  "enable_space_keepalive": {
    "title": "启用 Space 保活",
    "description": "定时 ping 服务",
    "hint": "定时访问 TTS 服务，降低 Hugging Face Space 休眠概率。",
    "type": "bool",
    "default": false
  },
  "space_keepalive_url": {
    "title": "额外保活地址",
    "description": "可选 URL",
    "hint": "留空则只保活 TTS 服务器列表。需要额外保活其他地址时填写。",
    "type": "string",
    "default": ""
  },
  "space_keepalive_interval_minutes": {
    "title": "保活间隔",
    "description": "分钟",
    "hint": "Hugging Face Space 通常一段时间无人访问会休眠，建议设置为 25 分钟左右。",
    "type": "int",
    "default": 25
  },
  "auto_tts_output_mode": {
    "title": "自动TTS输出模式",
    "description": "自动回复发送方式",
    "hint": "控制日常自动触发TTS时怎么发消息。audio_only=只发语音；audio_and_text=语音和原文都发；split_audio_text=前半段发语音、后半段发文字。",
    "type": "string",
    "default": "audio_and_text",
    "options": [
      "audio_only",
      "audio_and_text",
      "split_audio_text"
    ]
  },
  "llm_tool_tts_output_mode": {
    "title": "LLM工具输出模式",
    "description": "工具调用发送方式",
    "hint": "控制 genie_tts_speak 工具调用时怎么发消息。默认 audio_only 只发语音；也可改成语音和原文都发，或前半段语音后半段文字。",
    "type": "string",
    "default": "audio_only",
    "options": [
      "audio_only",
      "audio_and_text",
      "split_audio_text"
    ]
  },
  "send_text_with_audio": {
    "title": "语音附带原文",
    "description": "同时发送文字",
    "hint": "兼容旧版本配置使用。新版请改用上方的“自动TTS输出模式”。",
    "type": "bool",
    "default": true,
    "invisible": true
  },
  "tts_trigger_mode": {
    "title": "语音触发模式",
    "description": "控制生成频率",
    "hint": "always=每次回复都生成；interval=同一会话按时间间隔生成；random=每次回复按概率生成。",
    "type": "string",
    "default": "always",
    "options": [
      "always",
      "interval",
      "random"
    ]
  },
  "tts_trigger_interval_seconds": {
    "title": "触发间隔秒数",
    "description": "间隔模式使用",
    "hint": "仅当语音触发模式为 interval 时生效。同一会话距离上次触发超过该秒数才会再次生成语音；填 0 表示不限制。",
    "type": "int",
    "default": 300,
    "slider": {
      "min": 0,
      "max": 3600,
      "step": 10
    }
  },
  "tts_trigger_probability": {
    "title": "随机触发概率",
    "description": "随机模式使用",
    "hint": "仅当语音触发模式为 random 时生效。单位为百分比：0 表示不触发，100 表示每次触发。",
    "type": "int",
    "default": 30,
    "slider": {
      "min": 0,
      "max": 100,
      "step": 1
    }
  },
  "enable_sentence_splitting": {
    "title": "启用句子切分",
    "description": "长文本分段",
    "hint": "将长回复切成短句并行合成，可提升长文本生成速度，但会增加请求次数。",
    "type": "bool",
    "default": false
  },
  "sentences_per_chunk": {
    "title": "每段句子数",
    "description": "分段大小",
    "hint": "每个语音分段包含的句子数量。数字越小并行度越高，请求也越频繁。",
    "type": "int",
    "default": 2
  },
  "sentence_split_regex": {
    "title": "句子切分正则",
    "description": "切分表达式",
    "hint": "用于识别句子边界。括号用于保留标点，默认兼容中英文常见标点。",
    "type": "string",
    "default": "([。、，！？,.!?])"
  },
  "tts_max_text_length": {
    "title": "语音文本长度上限",
    "description": "过长截断",
    "hint": "只对送入 TTS 的音频文本生效：超过该字符数会优先在句子边界截断，避免超长回复被切成大量分块导致长时间排队。0 表示不限制；audio_and_text 模式下展示给用户的文字仍是完整原文，不受影响。建议 120-200。",
    "type": "int",
    "default": 150
  },
  "enable_custom_pause_marker": {
    "title": "自定义停顿标记",
    "description": "[pause=毫秒]",
    "hint": "开启后可在朗读文本中插入形如 [pause=600] 的标记来精确控制停顿（单位毫秒），Space 端会在该处插入静音、标记本身不会被读出；关闭时这些标记会被自动清除，不影响正常文本。单个停顿上限由 Space 的 GENIE_MAX_PAUSE_MS 决定（默认 3000）。",
    "type": "bool",
    "default": false
  },
  "custom_pause_prompt": {
    "title": "停顿标记提示词",
    "description": "注入说明",
    "hint": "仅当“自定义停顿标记”开启、且本轮确实注入了 LLM 提示词（自动TTS触发或工具调用）时才追加，用于告知 LLM 如何使用 [pause=毫秒] 标记；留空则不追加。",
    "type": "string",
    "default": "如需更自然的停顿，可在要朗读的句子中插入形如 [pause=600] 的标记（单位毫秒，最多 3000），合成时会在该处停顿相应时长，标记本身不会被读出。例如：$今日は[pause=600]またね$"
  },
  "tts_max_retries": {
    "title": "TTS 最大重试次数",
    "description": "失败重试",
    "hint": "语音合成失败时的最大重试次数。每次重试会尝试所有可用服务器。",
    "type": "int",
    "default": 3
  },
  "llm_injection_settings": {
    "title": "LLM 提示词注入",
    "description": "情感与翻译标签",
    "hint": "用于外语 TTS 的文本准备。建议先用“外语TTS准备方式”在“主LLM注入标签”和“其它Provider翻译”之间二选一；这个选择也会同步影响 LLM 调用语音工具时的文本准备方式。",
    "type": "object",
    "items": {
      "translation_workflow": {
        "title": "外语TTS准备方式",
        "description": "翻译链路二选一",
        "hint": "llm_injection=仅在本轮自动 TTS 确实触发时，要求主 LLM 在回复末尾给出 $翻译$；provider_translation=不注入翻译标签，改由下方 AstrBot Provider 或外部翻译 API 负责翻译。两种方式都不影响下面的“生成情感标签”，情感标签由它自己的开关单独控制。这个选择也会同步影响 genie_tts_speak 工具。",
        "type": "string",
        "default": "llm_injection",
        "options": [
          "llm_injection",
          "provider_translation"
        ]
      },
      "enable_llm_emotion": {
        "title": "LLM 情感识别",
        "description": "生成情感标签",
        "hint": "开启后会注入提示词，要求 LLM 在回复末尾添加 [emotion=xxx] 标签。两种“外语TTS准备方式”下都生效：provider_translation 链路会先剥离标签再送去翻译。前提是角色已用 /注册感情 注册过情感，且下方 Prompt 保留 {emotions} 占位符，否则会回落默认情感。",
        "type": "bool",
        "default": false
      },
      "llm_emotion_prompt": {
        "title": "情感识别提示词",
        "description": "注入 Prompt",
        "hint": "{emotions} 会替换为当前角色可用的感情列表。",
        "type": "text",
        "default": "# 情感标记\n你必须在每次回复末尾添加 [emotion=xxx] 标签来表示你的情感。\n可用情感: {emotions}\n（请从上述列表中选择一个最合适的）"
      },
      "enable_llm_translation": {
        "title": "LLM 直接翻译",
        "description": "兼容旧配置",
        "hint": "旧版兼容字段。新版请改用上方“外语TTS准备方式”。",
        "type": "bool",
        "default": false,
        "invisible": true
      },
      "llm_translation_prompt": {
        "title": "翻译注入提示词",
        "description": "翻译 Prompt",
        "hint": "仅在“外语TTS准备方式=llm_injection”且本轮自动 TTS 触发时生效。用于要求主 LLM 直接给出可用于 TTS 的翻译文本。",
        "type": "text",
        "default": "# 翻译\n你必须在回复末尾用 $...$ 符号包裹对应的日语翻译。\n例：今天天气不错 $今日はいい天気ですね$"
      },
      "enable_llm_tts_tool_prompt": {
        "title": "注入语音工具提示",
        "description": "引导按需调用工具",
        "hint": "开启后会额外注入一段提示词，告诉主 LLM 仅在用户明确要求试听、发语音、念一句时才调用 genie_tts_speak 工具。",
        "type": "bool",
        "default": true
      },
      "llm_tts_tool_prompt": {
        "title": "语音工具提示词",
        "description": "工具调用 Prompt",
        "hint": "用于约束主 LLM 何时调用 genie_tts_speak。支持 {character} 和 {emotions} 占位符；插件还会自动补充当前可用情感和文本准备方式，要求 LLM 调用工具时优先填写 emotion_name。",
        "type": "text",
        "default": "# 语音工具\n当且仅当用户明确要求你直接发一条语音、说一句给他听、念一段内容、试听声音时，你可以调用 genie_tts_speak 工具。\n普通闲聊时不要调用这个工具，也不要因为用户只是提到声音、角色、语音功能就主动调用。\n当前语音角色: {character}\n可用情感: {emotions}\n调用时请传入适合直接朗读的完整句子，并保留句末标点；必须根据朗读内容从可用情感中选择最贴切的 emotion_name，只有完全无法判断时才允许留空使用默认情感。"
      },
      "use_astrbot_provider": {
        "title": "使用 AstrBot Provider",
        "description": "兼容旧配置",
        "hint": "旧版兼容字段。新版请改用上方“外语TTS准备方式”。",
        "type": "bool",
        "default": false,
        "invisible": true
      },
      "astrbot_provider_id": {
        "title": "AstrBot Provider",
        "description": "翻译提供商",
        "hint": "当“外语TTS准备方式=provider_translation”时优先使用它来翻译；自动情感模式下也会让它一起返回情感名。",
        "type": "string",
        "default": "",
        "_special": "select_provider"
      },
      "translation_prompt": {
        "title": "Provider 翻译提示词",
        "description": "系统提示词",
        "hint": "仅在“外语TTS准备方式=provider_translation”时生效。注意默认值写死了翻译成 Japanese，若“TTS目标语言”不是日语请手动改掉；留空则插件会按目标语言自动生成中文提示词。",
        "type": "text",
        "default": "Translate the following text to Japanese. Output only the translation, nothing else."
      }
    }
  },
  "translation_api": {
    "title": "外部翻译 API",
    "description": "OpenAI/Gemini 兼容",
    "hint": "在“外语TTS准备方式=provider_translation”时，若未配置 AstrBot Provider，可改用这里的外部翻译 API；也可作为 Provider 不可用时的备用链路。",
    "type": "object",
    "items": {
      "base_url": {
        "title": "API 地址",
        "description": "基础 URL",
        "hint": "例如：https://api.openai.com/v1",
        "type": "string",
        "default": ""
      },
      "api_key": {
        "title": "API 密钥",
        "description": "密钥",
        "hint": "外部翻译 API 的访问密钥。",
        "type": "string",
        "default": ""
      },
      "model": {
        "title": "模型名称",
        "description": "翻译模型",
        "hint": "例如：gpt-4o-mini、gemini-1.5-flash。",
        "type": "string",
        "default": "gpt-4o-mini"
      },
      "api_format": {
        "title": "API 格式",
        "description": "接口类型",
        "hint": "选择外部翻译 API 的请求格式。",
        "type": "string",
        "default": "openai",
        "options": [
          "openai",
          "gemini"
        ]
      },
      "prompt": {
        "title": "翻译提示词",
        "description": "系统提示词",
        "hint": "发送给外部翻译 API 的系统提示词。",
        "type": "text",
        "default": "请将以下中文文本翻译成日语。只输出翻译结果，不要添加任何解释或其他内容。"
      },
      "w_mode_prompt": {
        "title": "自动情感提示词",
        "description": "翻译并选情感",
        "hint": "自动情感识别模式使用。无论你走 AstrBot Provider 还是外部翻译 API，这个模板都会用于“翻译并选择情感”。{emotion_list} 会替换为感情列表，{text} 会替换为原文。",
        "type": "text",
        "default": "请对以下中文内容进行翻译和情感分析。首先翻译成日语，然后从以下情感列表中选择最合适的一个：{emotion_list}。请按以下格式输出：\n[翻译后的日语文本][选择的情感名]\n\n原文：{text}"
      }
    }
  },
  "enable_tts_leak_guard": {
    "title": "参考音频泄漏防护",
    "description": "异常长音频重试",
    "hint": "开启后，若合成结果的时长明显超过文本预期（Genie 在极端情况下会把参考音频拼进结果，表现为“上一次的语音 + 这次的语音”叠成一条），插件会丢弃该结果并自动重试一次，最终保留较短的那份。建议保持开启。",
    "type": "bool",
    "default": true
  },
  "tts_leak_guard_seconds_per_char": {
    "title": "泄漏防护每字秒数",
    "description": "预期时长系数",
    "hint": "泄漏防护用来估算音频的预期上限时长：预期秒数 = 可发音字符数 × 本值 + 停顿标记总时长。值越小越严格、越容易触发重试。低于 0.2 会被自动修正为 0.2。语速偏慢的角色可以适当调大。",
    "type": "float",
    "default": 0.9
  },
  "tts_leak_guard_min_seconds": {
    "title": "泄漏防护最小基线",
    "description": "短文本兜底秒数",
    "hint": "预期时长的下限，避免极短文本（例如一两个字）因为固有的起止静音被误判成泄漏。低于 1.0 会被自动修正为 1.0。",
    "type": "float",
    "default": 3.0
  },
  "enable_state_persistence": {
    "title": "开关状态持久化",
    "description": "重启不丢设置",
    "hint": "开启后，会话/群聊的语音开关、自动情感模式以及各会话选择的角色与感情会写入插件 KV 存储，AstrBot 重启后自动恢复。关闭则回到旧行为：所有运行时开关在重启后重置为默认值。",
    "type": "bool",
    "default": true
  },
  "enable_tts_failure_notice": {
    "title": "失败提示追加到回复",
    "description": "回复末尾提示",
    "hint": "开启时，语音合成或发送失败会在文本回复末尾追加一行形如 (TTS失败: 原因) 的提示，方便排查。关闭后失败只写日志，不打扰聊天内容。",
    "type": "bool",
    "default": true
  }
}
