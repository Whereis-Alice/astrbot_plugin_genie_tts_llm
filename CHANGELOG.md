# 更新日志

## v1.9.7

- **[修复] 展开 / 折叠合成记录时页面会自己往上跳**：点一下「展开」，视口经常直接蹿到别的地方，想接着看下一条得重新往下翻。原因是展开态一变就 `renderLogs()` 整页重画：`clear(view)` 先把内容全删掉，文档瞬间变短，浏览器立刻把 `scrollTop` 夹到新的最大值（往往就是 0），等内容补回来滚动条已经回页首了。现在展开 / 折叠只替换那一条记录的节点，并按这一行在视口里的位置回填滚动量 —— 点过的那行钉在原地不动，键盘焦点也留在它上面。
- **[修复] 自动刷新会每 5 秒把你弹回页首**：同一个成因。日志页是全站唯一会自己定时重画的分区，开着自动刷新往下翻记录时，每一轮 tick 都把视口打回顶部。现在日志页所有重画（自动刷新、手动刷新、改筛选条件、搜索防抖回调）都走保位版本：重画期间先钉住容器高度让文档别缩，画完再把滚动位置写回去。切换子视图（合成记录 / 运行日志 / 情感统计）仍然明确回到页首 —— 那是换内容，本来就该从头看。
- **[修复] 界面偏好从来没被恢复过**：每次打开工作台，主题都退回「月夜」、密度退回「宽松」、上次停在哪个分区也不记得。日志里只有一条 DEBUG：`读取界面偏好失败: PluginKVStoreMixin.get_kv_data() missing 1 required positional argument: 'default'`。`get_kv_data(key, default)` 在这些 AstrBot 版本里 `default` 是**必填位置参数**（不是带缺省值的关键字参数），插件只传了 key，于是读取每次都抛异常、每次都吞掉、每次都回落到默认值 —— 保存那一侧一直是好的，写进去的偏好只是从来没被读出来。现在按签名补上 `default`，并把这条日志从 DEBUG 提到 WARNING（读不回来只影响记忆、不影响功能，但不该查不到原因）。
- **[新增] 运行日志着色**：以前一屏运行日志是清一色的灰字，要在几十行里找出「哪条是情感、哪条是合成、哪条炸了」只能逐行读。现在分三层上色：
  - **按分类给色相**：14 个分类各一个 hue（合成 / 分段停顿 / 情感 / 提示词 / 翻译 / 感情库 / 感情包 / 服务器 / 队列 / 触发 / 会话 / 生命周期 / WebUI / 其它），左侧细条、分类名前的小圆点、分类名本身共用它 —— 扫一眼就能按颜色成组。`WebUI` 一类是工作台自己戳接口产生的噪音，故意压成灰蓝，不跟真正的业务日志抢注意力。
  - **级别优先**：WARNING / ERROR / CRITICAL 的左条特异性更高，会盖掉分类色，出问题的行永远优先显示成黄 / 红，不会被分类色淹掉。
  - **正文分词**：`[会话 ID]`、`[emotion=…]` 这类方括号 ID，`「」` / `“”` 里的原话，成败关键词（失败 / 超时 / 放弃 vs 成功 / 命中 / 就绪 vs 跳过 / 重试 / 截断），文件路径与标识符（`tts_engine.py:441`、`emotions.json`、`aino/normal`、`Worker-3`），全大写缩写（`TTS`、`LLM`），带单位的数字（`1420ms`、`12.5KB`、`3 次`）各自单独上色；普通拉丁词只换等宽字体、不换颜色，避免整行花成圣诞树。
  - 六套主题共用一份色表：所有分类色都用 `color-mix` 往正文色里兑，暗色主题自动提亮、亮色主题自动压深，不用为每套配色各写一遍。
  - 顺带把级别 badge 定宽（分类与正文对齐成列，行首不再左右跳）、加了一层极淡斑马纹。
  - 「筛运行日志」卡片里多了一个**着色**开关，关掉就退回纯文本；这个选择和主题、密度一样会被记住。着色纯粹是前端渲染，复制 / 下载 / 搜索走的都是原始数据，导出的文本与真实日志逐字符一致。

## v1.9.6

- **[变更] 合成失败不再往聊天里插提示**：以前 TTS 失败会在回复末尾挂一行 `(TTS合成失败)`，于是对话里会冒出「里面还有一只粉色小猪(TTS合成失败)」这种把内部管道直接摊给对面看的句子，角色扮演的沉浸感当场碎掉。`失败提示追加到回复`（`enable_tts_failure_notice`）默认改为**关**：失败时聊天里只是少一条语音、文字照发，别的什么都不加。v1.9.5 起完整的失败链路已经躺在工作台「日志」页（合成记录 + 运行日志 + 情感统计），聊天里那行提示纯属噪音。真要旧行为，去「配置 → 输出形态」把它打开。
- **[修复] 老安装不会因为「改了默认值」就自动生效**：AstrBot 会把插件首次加载时的默认值写进 `data/config/…_config.json`，所以光改模板默认值救不了已经装过的实例（配置文件里明明白白写着 `true`）。这次带了一次性迁移：升级后首次加载把这一项关掉并保存，同时写一个 KV 标记 —— 只做一次，之后你自己重新打开也不会又被它关掉。存盘失败则回滚内存里的值，保证行为和配置文件一致。
- **[修复] 有一条失败提示绕过了开关，而且从来不写日志**：「语音合成成功、但 AstrBot 主动发送音频失败」这条回退分支是硬拼的 `(TTS音频发送失败)`，既不看上面那个开关，也不打日志；更糟的是外层已经先把这次合成记成了「成功」，于是这类失败在日志页里根本查不到。现在它走同一个开关、补一条带会话 ID 的 WARNING，合成记录也会如实记成失败（原因：音频发送失败）—— 和 LLM 主动发语音那条链路的记法对齐。
- **[改进] 提示被隐藏时日志级别升到 WARNING**：提示不进聊天之后，日志就是这次失败唯一的出口，只有 WARNING 以上才能被日志页的「只看问题」一档筛出来。
- **[改进] LLM 主动发语音失败时也别让模型念技术细节**：那条链路是把失败原因当工具返回值交给模型的，原文写着「请检查翻译配置或日志」，模型很自然就把这句转述给了用户。现在这几条内部失败的返回值末尾都补了一句约束：只用角色口吻简短提一句没能发出语音，不要复述工具名、配置项和日志。模型自己传错文本或情感名那几种情况不受影响 —— 那些本来就该让它改了重试。

## v1.9.5

- **[新增] WebUI「日志」页：终于能看见 bot 为什么挑了这个情感**：以前情感选得不好只能干瞪眼 —— AstrBot 控制台里插件的日志和其它插件混在一起、刷得飞快，而「LLM 原话是什么、翻译成了什么、真正送进 TTS 的是哪串文本、最后落到哪个角色的哪个情感、是 LLM 自己选的还是关键词兜底的」这条链路根本没留痕。现在工作台多了一个 ▤ 日志页，三个子页各管一段：
  - **合成记录**：每次合成一行，展开后是完整链路 —— LLM 原文 / 翻译结果 / 真正送进 TTS 的文本 / 命中的参考音频与参考文本 / 情感来源（LLM 选择、关键词兜底、固定情感、随机）与当时的候选情感列表 / 分段数与段间停顿 / 输出方式 / 重试与尾部截断补偿 / 耗时 / 失败时的原因原文。对着这些字段就能判断是情感选错了、参考音频不对，还是文本在送出前就被改坏了。
  - **运行日志**：只收本插件自己打的日志，不掺别的插件；按级别（含「只看问题」一档，等于 WARNING + ERROR + CRITICAL）和分类（翻译 / 情感 / 提示词 / 触发 / 服务器 / 队列 / 分段 / 合成 / 会话…）筛，也能全文搜关键词。
  - **情感统计**：按「角色 · 情感」聚合、按失败率倒序排，直接回答「哪些情感不好」；同时给出调用次数、平均耗时、成功率。
- **[新增] 日志页配套操作**：关键词 / 级别 / 分类 / 状态 / 来源 / 角色多维筛选（筛选条件会一并交给后端，不是前端假筛），5 秒自动刷新（输入框正在打字时自动让路，不会把光标刷掉），一键把当前一屏复制成纯文本，按当前筛选导出 `.txt`，以及清空缓冲。
- **[新增] 4 项配置（配置 → 日志与诊断）**：`enable_run_log`（总开关，默认开）、`run_log_capacity`（运行日志条数，50~5000，默认 500）、`run_log_synth_capacity`（合成记录条数，20~2000，默认 200）、`run_log_full_text`（关掉后只留 160 字摘要，省内存）。整套日志是纯内存环形缓冲，**不写盘**，重载插件即清空 —— 不会悄悄在磁盘上堆日志文件，也不会把 LLM 原文留到本次会话之后。
- **[新增] 日志页签带角标**：有合成失败/跳过时显示红色条数，只有 WARNING 以上日志时显示黄色条数，都没有就显示当前缓冲里的合成条数 —— 不用点进去也知道有没有事发生。

## v1.9.4

- **[修复] WebUI 导入感情包：粘完 JSON 按钮还是灰的**：「感情包内容」输入框的 `oninput` 只把文本写进内部状态，不刷新按钮，于是手动粘贴或输入之后 `试运行 / 执行导入 / 直接写入 / 清空内容 / 格式化 JSON` 五个按钮仍是渲染那一刻的禁用状态，字数提示也一直停在「还是空的」，非得再去点一下合并模式或试运行开关才活过来（拖放文件那条路径本来就会重渲染，所以只有手动粘贴会撞上）。现在按钮和提示随输入实时刷新，并且只定点更新这几个节点，不整块重渲染，光标和焦点不会被吃掉。
- **[修复] 变更报告的五个等宽列装不下差异极大的内容**：新增 / 更新 / 跳过 / 移除 / 无效这五个桶的内容量能差出两个数量级 —— 常见形态是「4 个桶空着、1 个桶 1 条」，也可能是「一次新增 39 条、每条带一串 `reference_audio/xxx.ogg` 长路径」。等宽固定列在这两头都难看：空列白占一大片，满列被挤成一行行省略号。现在改成：
  - 空桶不再建列，收成底下一行灰色 pill（`跳过 0`、`移除 0`…）；五个桶全空时直接写明「这份包对现有数据毫无影响」；
  - 有内容的列按条目数分三档抢宽度，一个桶独占时能吃满整行；
  - 每条拆成「角色 · 感情 / 变化原因 / 新旧值」三行竖排，长路径按字符断行 + 两行截断，完整新旧值挂在鼠标悬停提示上；
  - 超过 10 条先折叠，点「展开全部 N 条」之后才在列内滚动，不让一份大报告把整页顶飞。
- **[优化] 报告里终于能看出「动的是哪条参考音频」**：以前 `新增 / 跳过 / 移除` 只报「角色 · 感情」，一次导入 39 条时根本看不出每条指向什么；`更新` 那栏则写死一句「文本/语言有变」，参考音频前缀又都一样，截断后两边显示得一模一样，等于什么都没说。现在导入报告会把具体条目一起带回来，并按桶分别渲染：
  - `新增` 写出新条目的参考音频文件名、语言和参考文本；
  - `移除`（替换模式）写出即将被清掉的原值，动手之前能看清丢的是什么；
  - `跳过` 明确写「已存在，保留现有值」，并把被忽略的包内值并排摆出来，方便判断要不要改成覆盖模式；
  - `更新` 逐字段比对，只列真正变了的字段，路径只比文件名（`a.wav → b.wav`），完整值仍可悬停查看。

## v1.9.3

- **[修复] 整句凭空消失、结尾少一句：把重采样搬到真正便宜的那一层**：v1.9.2 已经会重采样了，但重试代价太高，预算只给得起 4 次尝试，实际不够用。这次量出了准确的失败率与代价结构，改在 decode 层重抽：
  - **退化率远高于上版的估计**：病例句 `こんにちは。` + `isla_01_calm` 实测单次 decode 退化率约 **60%**（退化形态是第一步就吐结束符，semantic token 数 = 0，该句段一点音频都没有）。v1.9.2 的 4 次尝试意味着整句丢失概率约 1/6——正好对应用户遇到的频率。
  - **旧的重试贵在无关的部分**：v1.9.2 的重试点在 `_synthesize_segment`，一次重试会把整个 `tts()` 重跑一遍，包括 encoder、BERT 和参考音频 SSL——那才是耗时大头，而它们的输出本来就是可复用的。所以预算只能给到 4 次。
  - **改到 decode 层重抽（根治）**：encoder 只算一次，之后只重跑 AR 解码。退化的那一次在解码循环第 1 步就退出，实测一次重抽约 2s，而一次成功的解码约 5s——重抽几乎免费，于是预算可以给到 **13 次尝试**（`GENIE_T2S_DECODE_RETRIES`，默认 12 次重抽）。按 60% 退化率，13 次全败的概率约 0.13%，再叠上下面的插件端兜底基本可以忽略。
  - **线上实测**：病例短句连打 8 次全部拿到音频（改前的期望是空手 1-2 次），三句一次请求打 6 次、每次都完整保留 2 段句间停顿（无丢句），共 15 次请求 0 失败。Space 日志里能看到 `T2S recovered on decode attempt N/13`，本轮观测到的最坏情况是第 4 次抽中。
  - 用户点停止时立刻返回，取消不消耗重抽预算，也不会被误当成退化。
- **[修复] 后端整段合成失败时插件根本不重试**：`_attempt_synthesis_on_server` 里那个「最多合成两次」的循环，只在**首次结果时长异常**时才会走到第二次；首次就连一个字节都没拿回来（后端整段失败）时是直接 `return None`，也就是这条语音直接发不出去，而这恰好是上面那个退化最常见的表现形式。现在这种情况会自动重抽一次。
- **[新增] 合成统计新增「空结果重试」**：`/tts-status` 与 WebUI 工作台侧栏都会显示（为 0 时不占版面）。这个数字长期非 0 说明 Space 端还是旧版 `app.py`，提示文案里会直接写明该去更新。

> **本版本的根治项在 Hugging Face Space 侧（`app.py`）**：用本仓库推荐 Space 模板的用户请同步更新自己的 Space；v1.9.2 的「Space 端重采样」同样在 `app.py`，一次更新覆盖两个版本。只更新插件也能用——第 2、3 项都是插件侧——但整句丢失的概率仍然是旧的量级。

## v1.9.2

- **[修复] 语音结尾被截断、句子念不完，或整句凭空消失**：根因在 Space 端——T2S 的 ONNX 采样没有固定随机种子，同一句话偶尔会在第一步就吐出结束符，该句段合成不出任何内容，服务端只写了一行日志就把它静默丢掉。落在最后一段就是「最后一个字没说完就突然结束」，落在中间就是一整句凭空消失。HF Space 日志实测抽样失败率约 25%，且完全随机——重采样一定有效。这次三层一起修：
  - **Space 端重采样（根治）**：单个句段采空、或音频明显短于该句段应有的长度时，就地重新采样，最多 3 次（`GENIE_SEGMENT_RETRIES`）。短音频的判定阈值是 `GENIE_SHORT_RETRY_S_PER_CHAR`（默认 0.075s/可发音字，只对 4 字以上的句段生效，避免短句误判）。全部不合格时取最长的一次，绝不静默丢句。用户点停止时立即返回，取消不会触发重试。
  - **插件端截断防护（兜底）**：合成结果的时长低于文本应有的下限时重合成一次，取更接近合理区间的那一次。新增 `enable_tts_truncation_guard`（默认开）与 `tts_truncation_guard_seconds_per_char`（默认 0.10s/字，日语实测最快 0.148s/字，取一半留足余量，只抓量级异常）。
  - **尾部补静音**：成品语音末尾补一小段静音，新增 `tts_tail_padding_ms`（默认 180ms，0 为关闭）。Space 有意丢掉每次请求末尾的句间停顿，最后一个音几乎贴着文件结尾，再叠上 QQ 把 WAV 转成 silk 时的收尾，尾音听起来就像被硬切。
  - **偏长与偏短统一打分**：原来只判「异常偏长＝疑似参考音频泄漏」。现在过长、过短、合格用同一套评分排序，「两次都偏长取更短的」「两次都偏短取更长的」由同一个比较式覆盖，两个防护不会互相打架；首次结果偏短时也不会被丢成「合成失败」，缺几个字总好过整条语音发不出去。
- **[修复] `[pause=ms]` 标记本身被当成了要朗读的内容**：`pause` 和后面的数字都落在可发音字符集里，于是「只有一个停顿标记的消息」会被判成有内容并送去合成——那正好是触发参考音频泄漏的输入；带标记的正常句子也会把可发音字数算多，让新加的截断防护误判偏短、白白重试一次。现在统计可发音字符前会先剥掉标记本身。
- **[修复] 长回复的后半段被静默丢掉，听起来同样像「话没说完」**：这是与上面完全独立的第二个成因，而且是必然发生而非随机。`tts_max_text_length` 之外的文本会被直接丢弃、永远不会被朗读，而它的默认值只有 **150 字**——中文回复翻成日文后字数普遍要涨，一段稍长的回复很容易在半路被砍掉。旧的截断点还把句末标点和逗号顿号混在一个字符集里从后往前找，于是经常正好切在逗号上，听起来就是标准的「话说到一半断了」。触发时只写了一条 `info` 日志，用户根本看不到。这次三处一起改：
  - 默认上限 `150 → 300`，够装下绝大多数一次性回复，又不会让一条消息排出十几个分块；`0` 仍然表示完全不限制。
  - 截断点分两档优先级：**先找句末标点**（`。！？!?…．.` 与换行），并把紧跟其后的 `」』）` 等收尾符号一起带上；只有当句末标点太靠前（会丢掉一半以上内容）时才退让到逗号顿号一类的软停顿，**并把软停顿本身去掉**，让后面的补句末标点逻辑给它接上句号，而不是留下「……，。」这种别扭的组合；两档都不可用才硬切。
  - 触发截断时日志从 `info` 升级为 `warning`，明确写出「超长部分不会被朗读」和该调哪个配置；同时新增 `text_truncated` 统计，`/tts-status` 与 WebUI 工作台侧栏都能看到次数和当前上限，工作台的流水线预览也会写明后面多少字不会被朗读。
- **[新增] 截断拦截次数进入统计面板**：`/tts-status` 的合成统计与 WebUI 工作台侧栏都会显示，工作台的运行参数卡新增「尾部静音」，状态区新增「截断防护 开/关」标记。
- **[优化] 新增的 3 项配置已归入 WebUI 配置页分组**：原「泄漏防护与持久化」改名为「音质防护与持久化」，同时容纳泄漏与截断两类防护，不会被塞进「其它」分组。

## v1.9.1

- **[修复] WebUI 工作台点标签不切换、内容全堆在一页**：`.view` 自带 `display: flex`，作者样式表的类选择器盖掉了浏览器内置的 `[hidden] { display: none }`，导致 JS 已经把非当前分区标成 `hidden`、页面上却 8 个分区一起显示，看起来就像「点了别的模块，内容全跑到页尾去了」。现在在样式表最前面加了 `[hidden] { display: none !important; }` 全局兜底。
- **[修复] 批量操作条在没有勾选时仍然显示**：感情库「已选 N 条 / 导出所选 / 批量删除」那一条用的 `.row-tight` 也带 `display: flex`，属于同一个根因，一并修好。
- **[修复] 侧栏「运行统计」与底部状态栏计数不一致**：合成完成后只刷新了状态栏，侧栏 RUNTIME 卡还停在打开页面时的快照（会出现「总请求 2 / 成功 0」对「请求 3 · 成功 1」这种对不上的情况）。现在统计卡改成局部刷新，合成成功与失败都会同步，而且不再整体重渲分区——正在播放的试听音频不会被打断。
- **[优化] 切换分区时把滚动位置拉回顶部**：从很长的分区（比如感情库）切到别的分区，不用再自己往上滚。
- **[优化] 离线冒烟测试新增回归断言**：切分区后校验「有且仅有一个分区可见」、校验滚动复位真的发生，并对样式表做静态检查确保 `[hidden]` 兜底规则不会再被删掉。

## v1.9.0

- **[新增] WebUI 语音合成工作台**：AstrBot WebUI 的插件页里多了一整套可视化面板，不用记指令也能把插件玩明白。后端新增 24 个接口，全部走 AstrBot 自带的插件页桥接，不额外开端口、不引入新依赖。八个视图：
  - **合成台**：选角色 / 感情、写文本、直接在页面里合成并试听；「分段预览」能看清句子会被怎么切分、每段补多少停顿、哪些片段因为没有可发音字符被跳过。
  - **感情库**：表格化管理所有角色与感情，支持新增 / 编辑 / 删除 / 复制到别的角色 / 整个角色改名，每行都能一键试听。
  - **感情包**：导出下载 `.json`、粘贴文本导入（含三种模式与试运行）、服务端快照的保存与恢复。
  - **配置**：9 组共 35 项配置带说明与校验，需要重启才生效的项单独标注；密钥类字段回显为掩码，不改就不会被覆盖。
  - **服务器**：并发探测每台 TTS 服务器的连通性、延迟、可用角色，以及 Space 保活状态。
  - **会话**：列出当前开了语音的会话与群，可直接在页面上开关。
  - **指令**：全部指令与别名速查，按「开关 / 音色 / 合成 / 感情包 / 诊断」分组。
  - **关于**：版本、数据目录、感情统计与合成计数。
- **[新增] 6 套 galgame 风格主题**：月夜（默认，深色）· 樱色（浅色）· 黄昏（深色）· 苍海（深色）· 薄雪（浅色）· 绯夜（深色），另有「舒适 / 紧凑」两种信息密度。主题与密度保存在插件的 KV 存储里而不是浏览器本地，换浏览器、换设备都能保持一致。
- **[新增] 感情包导入导出（指令与 WebUI 双通道）**：新增 `/感情导出`、`/感情导入`、`/感情包`、`/感情包保存`、`/感情包恢复` 五条指令。导出时会给出摘要、在服务端存一份快照，QQ 平台还会直接把 `.json` 文件发给你；导入支持三种合并模式（`merge` 只补新 / `overwrite` 覆盖同名 / `replace` 清空重建）与「试运行」预演，JSON 可以贴在正文里、引用一条含 JSON 的消息，或者上传 `.json` 附件。写入前逐条校验参考音频路径（禁止绝对路径与 `..`）与语言字段，不合法的条目会被跳过并在摘要里说明；导入成功立即生效，不需要重启。
- **[修复] 贴在代码块里的感情包导入失败**：指令原文里的换行此前会被压成空格，Markdown 代码围栏因此剥不掉，直接报「JSON 解析失败」；同时 AstrBot 的指令过滤器会把正文的第一个 token（代码围栏那一行）当成「模式」参数塞进来，于是又误报「导入模式只能是…」。现在导入会保留原文换行、按任意空白切参数、只采信真正的模式词，围栏无论是否被平台压成一行都能剥掉。写错的模式词（如 `/感情导入 xxx {...}`）则会明确提示模式非法，而不是抛出难懂的解析错误。
- **[改进] `emotions.json` 读写更抗造**：保存改为「同目录临时文件 + fsync + 原子替换」，磁盘满或进程被杀不会再留下半个 JSON；加载容忍 UTF-8 BOM（用记事本另存过的文件不再让整库「凭空消失」）；文件损坏或顶层不是对象时，会自动留一份 `emotions.corrupt-<时间戳>.json` 备份（同样内容只留一份）并提示可用 `/感情包恢复` 找回，避免坏文件被下一次写入静默覆盖。
- **[改进] `/查看感情` 支持按角色过滤**：`/查看感情 kisaki` 只看一个角色，末尾附上「共 N 个角色 / M 条感情」统计，并给出导入导出与工作台的入口提示。
- **[改进] 插件页中英双语**：新增 `.astrbot-plugin/i18n/zh-CN.json` 与 `en-US.json`，工作台的标题与入口会跟随 AstrBot 的语言设置。

## v1.8.1

- **[修复] 开启句子切分后停顿全部消失（“一口气念完”）**：一条消息被拆成多次请求再拼接时，Space 只在同一次请求的段落之间插静音、会丢掉每次请求末尾那一拍，而插件是逐块裸拼，于是分块边界处的停顿直接归零。现在拼接时按上一块的结尾标点补回静音：句末标点 / 省略号补满，逗号顿号补一半，切在句子中间（自定义切分正则可能不带标点）不补，硬塞静音只会造成结巴感。新增配置 **分段间停顿** `chunk_gap_ms`（默认 260ms，0 表示不补，上限 2000）。
- **[修复] 行首或独立成行的省略号会丢掉停顿**：`……そうね。` 开头那一拍此前被整段丢弃，`そう。` / `……` / `そうね。` 分行写时停顿又落到了错误的一侧。根因是被丢弃的纯标点段所欠的静音被折算给了**下一**段，而一个段落的停顿本质上属于它**之后**的那段静音。现已改为折算给前一个保留段，并把「出现在最前面」「出现在最末尾」两种边界单独处理。
- **[修复] 中间某个分段合成失败时会吞掉停顿**：失败的段落直接跳过，它欠的静音一起消失，前后两句被粘成一句。Space 侧改为「静音缓冲，只在真正有音频要写之前 flush」，失败段落的停顿会累加到下一段之前；整段全部失败时仍返回 0 字节，插件「0 字节 = 合成失败」的判定不受影响。
- **[改进] 参考音频泄漏防护的预算算入自动停顿**：预期时长此前只加上显式 `[pause=ms]` 标记的时长，句末标点与省略号带来的自动停顿没算进去，停顿密集的短文本可能被误判成「参考音频被拼入结果」。现在按句末标点 260ms、省略号 420ms 一并计入预算。

> **第 2、3 项修复在 Hugging Face Space 侧（`app.py`）**：用本仓库推荐 Space 模板的用户请同步更新自己的 Space，否则行首省略号与失败分段的停顿仍是旧行为。第 1 项与第 4 项只需更新插件。

## v1.8.0

- **[修复] 主动发语音偶发“上一条语音 + 这次语音”拼成一条**：根因在 Genie 后端 `genie_tts/Core/Inference.py` 的 `GENIE.t2s_cpu()`，结尾返回 `np.expand_dims(y[:, -idx:], axis=0)`。当模型第一步就吐出 EOS（纯标点段必然如此）时 `idx == 0`，`y[:, -0:]` 会退化成**整个序列**，把参考音频的 prompt token 一起解码出来，于是结果变成「完整参考音频 + 本次语音」。已在 Hugging Face Space 侧修补该函数：按 `prompts` 实际长度切片、限制最大 token 数、无有效语义 token 时直接报错；同时修掉了「抹除 EOS 导致上层截断失效」以及 `-idx` 偏移吃掉约 40ms 起音的问题。**自建后端的用户请同步更新 Space / 本地部署。**
- **[新增] 参考音频泄漏防护（插件侧兜底）**：新增 `enable_tts_leak_guard`（默认开启）。插件按「可发音字符数 × 每字秒数 + 停顿标记时长」估算音频的合理上限，返回音频量级异常偏长时丢弃并自动重试一次、保留较短的那份，并在日志打出 `疑似参考音频被拼入结果`。阈值可用 `tts_leak_guard_seconds_per_char`（默认 0.9）与 `tts_leak_guard_min_seconds`（默认 3.0）调整。即使后端没更新，也不会再把叠加音频发给用户。
- **[修复] 纯标点 / 表情文本仍会请求 TTS**：新增可发音字符检测，`……`、`。`、`！？`、`♪`、`ーー`、`♡`、emoji 这类没有实际读音的内容不再送入合成（三条合成入口全部前置短路）；句子切分时也会把不可发音的碎片合并进相邻句子，避免后端拿到纯标点分段——这正是触发上面那个 bug 的路径。后端返回 0 字节时按失败处理，不会再发出一条 44 字节的空语音。
- **[修复] `/ttg-q` 在「默认开启全部群语音」或群白名单下会被重新打开**：此前关闭群语音只是把群号从激活集合里移除，重启（或重载插件）后又被白名单 / 默认开关重新激活。现在会同时写入 inactive 集合并持久化，关闭意图不再被覆盖。
- **[新增] 开关与音色选择持久化**：新增 `enable_state_persistence`（默认开启）。会话/群语音开关、自动情感模式、各会话选择的角色与感情会写入插件 KV 存储，AstrBot 重启后自动恢复；旧版本 AstrBot 没有 KV 接口时自动降级为旧行为，不会报错。
- **[新增] `/tts-status`（`/语音状态`）**：一条指令看清本会话与本群的开关状态、当前音色是否可用、触发模式、两种输出模式、合成统计（成功 / 失败 / 无朗读内容跳过 / 泄漏拦截）与排队数量，并并发探测所有 TTS 服务器的连通性、延迟和可用角色列表。
- **[新增] `/tts-again`（`/重发语音`）**：重发本会话最近一条合成成功的语音，不再消耗一次 TTS 算力；临时文件已被清理时会明确提示。
- **[新增] `/tts-help`（`/语音帮助`）**：列出插件全部指令与别名。
- **[新增] `enable_tts_failure_notice`**：默认维持原行为（失败时在回复末尾追加 `(TTS失败: ...)`）；关闭后失败只写日志，不打扰聊天内容。
- **[修复] 失败提示残渣泄漏到聊天**：清理 LLM 复述的幻觉提示时用的是 `replace("(TTS失败: 角色", "")` 这类片段匹配，会留下 `'xxx'无效)` 之类的残字。现在改为整条正则匹配清理，并在清理后正确回写文本。
- **[改进] 插件元信息与 Logo**：新增 `logo.png`（AstrBot WebUI 插件卡片会自动显示）与 `.gitignore`；`metadata.yaml` 补充 `short_desc` 与 `astrbot_version: ">=4.16,<5"`。
- **[改进] 临时文件与后台任务清理**：合成失败、被取消或结果被丢弃时立即删除临时 WAV；清理协程与状态恢复任务在插件卸载时会被正确取消，不再留下悬挂 task。

## v1.7.1

- **[修复] provider_translation 链路下不注入情感标签**：此前只要开着翻译且“外语TTS准备方式=provider_translation”，即使打开“生成情感标签”也不会注入 `[emotion=xxx]` 提示词，导致自动语音永远落到会话固定/默认情感。现在情感标签与翻译链路彻底解耦，只由“生成情感标签”开关控制；provider 链路会先剥离标签，再把净化后的原文交给翻译 Provider，不会污染翻译输入也不会漏进聊天。
- **[改进] 翻译系统提示词留空兜底**：`translation_prompt` 被清空时不再发送空系统提示词，而是按“TTS目标语言”自动生成中文提示词（保留语气与句末标点、只输出译文）。
- **[文档] 配置项说明修正**：`translation_workflow`、`enable_llm_emotion`、`translation_prompt` 的 hint 同步为新行为。

## v1.7.0

- **[新增] 语音停顿控制与自定义 `[pause=ms]` 标记**：上调 Space 端句间 / 省略号停顿时长（可用环境变量 `GENIE_SENTENCE_PAUSE_MS`、`GENIE_ELLIPSIS_PAUSE_MS`、`GENIE_MAX_PAUSE_MS` 调整）；新增“自定义停顿标记”开关，开启后可在朗读文本中用 `[pause=600]` 精确插入静音，关闭时会自动清除标记且不朗读。
- **[新增] 语音文本长度上限 `tts_max_text_length`**：超长回复送入 TTS 前按句子边界优先截断（默认 150，0 表示不限制），避免被切成大量分块导致长时间排队等待；`audio_and_text` 模式下展示给用户的文字仍是完整原文。
- **[新增] TTS 服务器计时日志**：每次合成会记录参考音频设定耗时、首包响应耗时、合成传输耗时与音频时长，方便定位卡顿与超时。
- **[修复] “老是默认情感”排查**：角色未注册任何情感时会 `warning` 提示（可用 `/注册感情` 补注册），本轮不会再要求 LLM 输出 `[emotion=xxx]`；未解析到情感标签时也会 `info` 记录，便于排查三种输出模式下的实际情感来源。
- **[修复] 文本清洗覆盖停顿处理**：文本清洗现在基于已处理文本进行，不再把 `[pause]` 标记的清除结果覆盖回原始文本。
- 感谢源仓库 [clown145/astrbot_plugin_tts_llm](https://github.com/clown145/astrbot_plugin_tts_llm)。

## v1.6.7

- **[修复] llm_injection 缺少翻译标签时污染回复**：当本轮自动 TTS 触发但主 LLM 没有返回 `$...$` 翻译标签时，插件会记录日志并跳过本次语音合成，不再向聊天追加 `(TTS失败: 翻译无结果)`。

## v1.6.6

- **[优化] LLM注入按需触发**：`llm_injection` 的 `$翻译$` 与 `[emotion=xxx]` 只会在本轮自动 TTS 确实触发时注入；interval/random 跳过的轮次不会再强迫主 LLM 生成隐藏标签。
- **[修复] 标签泄露兜底**：LLMResponse 阶段和发送前结果装饰阶段都会清理残留的 `$翻译$` 与 `[emotion=xxx]`，减少调用其它工具时标签直接暴露到聊天里的情况。
- **[优化] 语音工具情绪选择**：`genie_tts_speak` 的运行时提示会始终补充当前角色可用情感，并要求 LLM 调用工具时优先填写 `emotion_name`。

## v1.6.5

- **[优化] 自动TTS情绪日志**：自动触发语音时会记录本次实际使用的角色、情感、情感来源和参考音频，方便排查 always/interval/random 模式下到底选了哪个情绪。
- **[修复] 情绪回落日志**：当 LLM 或 Provider 返回了不存在的情绪时，会明确记录回落到默认情绪，避免日志里看不出实际合成使用了哪个参考音频。

## v1.6.4

- **[修复] 语音工具提示占位符**：`llm_tts_tool_prompt` 现在支持 `{character}` 与 `{emotions}`，手动改过 Prompt 的用户也可以明确告诉 LLM 当前角色和可用情感。
- **[修复] 工具提示注入范围**：即使当前会话未开启自动 TTS，只要语音工具提示开关开启，插件也会注入 `genie_tts_speak` 的调用约束和情感提示，方便用户临时要求 Bot 发语音。

## v1.6.3

- **[修复] LLM 工具语音尾音截断**：TTS 合成前会自动补齐句末标点，降低 Genie 后端分句时最后一句被截断或没说完的概率。
- **[修复] LLM 工具总是默认情感**：工具提示会注入当前角色可用情感，并要求 LLM 调用 `genie_tts_speak` 时优先填写 `emotion_name`。
- **[优化] 工具文本标签兜底**：`genie_tts_speak` 会解析工具文本中的 `[emotion=xxx]` 与 `$...$`，即使 LLM 把标签塞进 `text` 参数，也能尽量使用正确情感和翻译文本。
- **[优化] 翻译总开关文案**：将“启用翻译功能 / 合成前翻译”调整为“启用目标语言转换 / 外语TTS总开关”，减少与独立 Provider 翻译模式的概念冲突。

## v1.6.2

- **[调整] 外语TTS准备方式二选一**：新增 `translation_workflow` 配置，统一为 `llm_injection` 和 `provider_translation` 两种外语语音准备模式，避免主 LLM 注入翻译与独立翻译 Provider 混用。
- **[修复] LLM 工具调用翻译链路**：`genie_tts_speak` 现在会同步遵循外语TTS准备方式。注入模式下直接使用主 LLM 提供的目标语言文本；Provider 模式下由插件负责翻译后再发起 TTS。
- **[修复] 语音工具提示注入时机**：即使未开启情感/翻译标签注入，只要开启了语音工具提示，插件也会正确向主 LLM 注入工具调用约束。
- **[优化] 自动情感识别的 Provider 路径**：在 `provider_translation` 模式下，自动情感识别会优先让 AstrBot Provider 或外部翻译 API 同时返回“翻译结果 + 情感名”。

## v1.6.1

- **[修复] 翻译注入与框架内翻译冲突**：开启“生成翻译标签”后，会自动忽略 AstrBot Provider 翻译，避免两套翻译链同时参与自动 TTS。
- **[修复] 关闭框架内翻译后的误报**：当自动 TTS 已使用主 LLM 直接生成翻译时，即使未填写外部翻译 API，也不会再输出“翻译API配置不完整”的无效报错。
- **[同步] 配置说明与文档**：更新配置页提示与 README，明确“生成翻译标签”与“框架内翻译”的优先级关系。

## v1.6.0

- **[新增] 自动TTS输出模式**：新增 `auto_tts_output_mode`，支持 `audio_only`、`audio_and_text`、`split_audio_text` 三种自动回复发送模式。
- **[新增] LLM工具输出模式**：新增 `llm_tool_tts_output_mode`，单独控制 `genie_tts_speak` 的输出方式，默认保持纯语音。
- **[修复] 语音吞字显示问题**：自动 TTS 与 LLM 工具的“语音+文字”改为分开发送，避免被 splitter 拆成“前半句进语音、后半句留文字”的错位效果。
- **[兼容] 旧配置迁移**：保留 `send_text_with_audio` 兼容旧版本配置，升级后会自动映射到新的自动TTS输出模式。

## v1.5.1

- **[新增] 可配置语音工具提示注入**：新增 `enable_llm_tts_tool_prompt` 与 `llm_tts_tool_prompt`，可引导主 LLM 仅在用户明确要求时调用 `genie_tts_speak`。
- **[优化] 注入日志**：日志中会额外记录本轮是否注入了语音工具提示。

## v1.5.0

- **[新增] LLM 主动语音工具**：新增 `genie_tts_speak` 工具，允许 LLM 在用户明确要求时主动合成并发送语音。
- **[优化] 自动语音与工具协同**：当 LLM 工具已主动发送语音时，插件会自动跳过本轮普通自动 TTS，避免重复发声。
- **[整理] README 更新日志**：README 中不再内嵌大段历史更新，统一跳转到 `CHANGELOG.md` 查看。

## v1.4.0

- **[调整] Fork 标识**：插件 ID、元数据、数据目录和文档已改为 `astrbot_plugin_genie_tts_llm`。
- **[优化] 配置说明**：将配置项长描述拆分为短 `description` 与详细 `hint`，改善 AstrBot WebUI 中说明被截断的问题。
- **[新增] 语音触发频率**：新增 `always`、`interval`、`random` 三种触发模式，可按时间间隔或概率减少语音生成频率。
- **[新增] 翻译调试日志**：新增开关，可在 AstrBot 日志中输出原文和最终送入 TTS 的文本。

## v1.3.7

- **[新增] 默认开启全部群语音**：新增 `enable_group_tts_by_default` 配置项。开启后所有群聊默认启用群组级语音合成，无需逐个配置白名单或执行 `/ttg`。
- **[优化] 群组开关优先级**：群组黑名单仍然最高优先级；开启“默认全部群”后，`/ttg-q` 可在本次插件运行期间临时关闭当前群，`/ttg` 可重新开启。

## v1.3.6

- **[新增] 全局 FIFO 请求队列**：TTS 请求改为严格先到先得，后到请求不会插队。
- **[优化] 队列工作器与退出清理**：新增请求调度 worker；插件卸载时会停止 worker 并清空未处理请求，避免遗留等待任务。
- **[优化] 长文本并发 worker 数量**：句子切分合成时，worker 数量限制为 `min(文本块数, 服务器数)`，减少空闲协程占用。

## v1.3.5

- **[新增] TTS 文本清洗配置**：新增 `enable_tts_text_cleaning` 与 `tts_text_clean_regex`，支持在发送 TTS 前按正则清洗文本（默认关闭）。
- **[修复] LLM 翻译标签提取误匹配**：仅在开启 LLM 翻译注入时提取翻译标签，并限制为显式结尾标记（`$...$`/`＄...＄`），避免误删普通文本中的路径、金额等内容。
- **[修复] 翻译异常二次报错**：修复翻译请求早期失败时 `response` 未定义导致的异常分支崩溃问题。
- **[修复] 插件卸载后台任务泄漏**：插件卸载时会主动停止 TTS 引擎清理协程，避免热重载后残留任务。
- **[优化] 嵌套括号清洗稳定性**：提高清洗轮数，改善深层嵌套括号文本的清洗效果。

## v1.3.4

- **【新增】群组白名单**：新增 `群组白名单` 配置项，白名单中的群组将在 Bot 启动时自动开启全局语音合成（无需手动执行 `/ttg` 指令）。
- **【新增】群组黑名单**：新增 `群组黑名单` 配置项，黑名单中的群组将完全禁用语音合成的所有功能和指令。
- **【说明】优先级**：如果某群组同时出现在白名单和黑名单中，黑名单优先（安全考虑）。

## v1.3.3
> [!IMPORTANT]
> **Hugging Face Space 需要重新部署**：本版本修复了后端 genie-tts 的兼容性问题。**请使用最新的 `Dockerfile` 和 `app.py` 重新复制/部署您的 Space**，否则中文 TTS 可能无法正常工作。

- **【修复】后端模型加载**：修复了 genie-tts 2.0.2 与 HuggingFace 模型仓库的兼容问题，中文合成现已正常工作。
- **【新增】翻译开关**：新增 `启用翻译功能` 配置项。如果您使用的是中文模型，可关闭此选项直接使用原文，无需翻译。
- **【新增】超时配置**：新增 `TTS 超时时间` 配置项（默认 120 秒），支持自定义单次 TTS 请求的超时时间。
- **【优化】配置界面**：重新组织配置页面，使用 emoji 分类，更加清晰美观。
- **【优化】Worker 优化**：并行合成时，Worker 数量现在取 min(服务器数, 文本块数)，避免创建多余的空闲 Worker。

## v1.3.2
- **【修复】后端兼容性**：修复了因新版 API 变更导致插件无法访问后端的问题（缺少 `language` 参数）。
- **【新增】多语言支持**：新增 `tts_default_language` 配置项，允许设置 TTS 默认语言（默认为 `jp`），支持 `jp`, `zh`, `en`。
- **【新增】语言注册**：`/注册感情` 指令现在支持可选的语言参数，允许为特定的感情单独指定语言（如 `zh`, `en`），未指定则使用默认配置。

<details>
<summary>历史更新日志</summary>

## v1.3.1
- **【优化】提示词增强**：默认注入的提示词增加了 Markdown 标题 (`##`)，提升了 LLM 遵循指令的稳定性。
- **【优化】容错性提升**：增强了对全角符号 (`＄`) 的兼容性，并增加了对 LLM 幻觉报错的自动清理机制。

## v1.3.0
- **【新增】群组语音模式**：新增 `/ttg` 指令，可开启群组级语音合成，对群内所有成员生效。
- **【新增】LLM 情感注入**：支持让主 LLM 直接生成情感标签（格式 `[emotion=xxx]`），比旧版插件单独分析更精准。
- **【新增】LLM 直接翻译**：支持让主 LLM 直接生成日语翻译（格式 `$xxx$`），省去额外翻译 API 调用。
- **【新增】框架模型翻译**：支持直接调用 AstrBot 框架内配置的模型进行翻译，无需在插件中重复配置 Key。
- **【优化】配置升级**：新增"LLM 注入与高级翻译设置"配置区，支持自定义注入提示词。

## v1.2.2
- **【优化】保活机制**：现在支持同时对所有配置的 TTS 服务器进行保活，并可额外配置独立的保活地址。
- **【优化】缓存自动清理**：新增自动清理任务，每小时清理一次超过 30 分钟的临时音频文件，防止磁盘占用过大。
- **【优化】健壮性提升**：优化了保活任务的异常处理，防止任务意外终止。

## v1.2.0
- **【新增】长文本快速合成模式**：在配置中增加"句子切分"开关。开启后，插件会将长回复切分成短句，并行发送给多个TTS服务器合成，最后拼接成一条语音，显著提高长文本的响应速度。
- **【优化】代码结构重构**：将插件代码按功能模块化拆分为多个文件，提升了代码的可读性和可维护性。

## v1.1.0
- **【新增】自动情感识别模式**：加入 `/tts-w` 指令，可调用 LLM 分析文本情感，并自动匹配最合适的已注册声音进行合成。
- **【新增】配套指令**：加入 `/sw-w` 用于在自动模式下切换角色，以及 `/tts-w-q` 用于单独关闭自动模式。
- **【优化】配置体验**：将 WebUI 中的"TTS 服务器地址列表"从手动输入 JSON 字符串改为更方便的列表形式，可轻松增删地址。
- **【优化】提示词可配置**：将自动情感识别模式的提示词移入配置文件，方便用户自定义。
- **【调整】指令逻辑**：`/tts-q` 现在会关闭所有语音合成模式。

</details>
